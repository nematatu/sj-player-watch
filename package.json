{
	"name": "sj-player-watch",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled",
		"types": "wrangler types",
		"typecheck": "tsc --noEmit",
		"test": "bun test"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"typescript": "^5.5.2",
		"wrangler": "^4.131.0"
	}
}
